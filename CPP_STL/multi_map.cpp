//C++ program to illustrate the concept of Multi-map
#include<iostream>
#include<bits/stdc++.h>
using namespace std;

int main()
{
    // Everything is the same as map, only it can store multiple keys
    // Sorted order of keys is also maintained
    // Only map[key] cannot be used here
    // tldr; it can have duplicate keys

    return 0;
}
