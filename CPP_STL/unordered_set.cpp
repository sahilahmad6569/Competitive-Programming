//C++ program to illustrate the concept of Unordered Set
#include<iostream>
#include<bits/stdc++.h>
using namespace std;

int main()
{
    unordered_set<int> a;
    // It stores Unique but not in Order
    // Random order
    // lower_bound() and upper_bound() function doesn't work
    // Rest all the funcionts are same
    // It doesn't store in any particular order
    // It has a better complexity
    // than set in most cases, except some when collision happens
    // Imp- It has better complexity than sets

    return 0;
}
